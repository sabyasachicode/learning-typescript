# Typescript setup
**Project initialization** `npm init`

**TS installation** `npm i -D typescript`

**TS execution** `npx tsc --init`

Then uncomment `"rootDir": "./src",
    "outDir": "./dist",` this two lines in tsconfig.json and create src and dist folder.
